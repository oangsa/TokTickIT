# Lab 2 — AI Use and Reflection  (fill this in)

**LLM/agent used:** Codex (GPT-5)

The canonical Lab 2 AI-use record is `docs/lab-02/ai-use.md`, as required by
the Lab 2 handout. Lab 1 retains its historical `docs/lab-01/ai_use.md`
filename; this distinction is intentional. This handout-facing record selects
40 representative prompts in 38 table entries from the Lab 2 history available
in the repository and current session.

## Selected key prompts (40; 38 entries)
| # | Prompt (summarised) | What I did with the result |
|---|---------------------|----------------------------|
| 6 | Synchronize the frozen Lab 2 contract with the final QueryBuilder boundary and unordered `attachmentIds` idempotency clarifications. | Updated the four Lab 2 contract documents to preserve the generic QueryBuilder/resource-validator/frontend boundary, define the Ticket condition matrix, normalize and sort Attachment IDs while rejecting duplicates, and align the planned tests. Reviewed the result as documentation-only work; no application implementation was started. |
| 10 | Revert only the rejected Lab 2 initial Attachment workflow redesign while preserving the newer approved decisions. | Restored standalone Pending pre-upload, sorted `attachmentIds`, replay-first idempotency, atomic Ticket/binding, orphan cleanup, compensation, unified lifecycle deletion, and synchronized tests/UI. Reviewed the change against the frozen `/api`, DTO, ownership-404, CORS, QueryBuilder, Playwright, and guarded-PostgreSQL decisions; no application code was changed. |
| 12 | Perform a focused Lab 2 documentation-contract correction: add the authoritative schema, explicit PROCESSING/COMPLETED idempotency claim model and SHA-256 contract, visible generated/context Create fields, automated UI-style assertions, and synchronized traceability without changing application code or reducing approved scope. | Updated the four contracts with exact Prisma/PostgreSQL types, defaults, restrictive FKs, checks, indexes, claim-before-mutation ordering, current-state replay tests, Create Ticket read-only generated/context fields, and AC/test evidence. Preserved modular test filenames and the approved API, Attachment, ownership, and tooling decisions. |
| 13 | Finalize stale PROCESSING recovery as a five-minute lease: fresh same-hash waits, stale same-hash atomically reclaims, different hash conflicts, reclaim resets `processingStartedAt`, and concurrent retries have one reclaim owner; add PostgreSQL concurrency evidence. | Updated the specification, API contract, schema field contract, unit/API/PostgreSQL test plan, AC-65 traceability, and Issue close gate with the exact 300-second boundary and PG-11 evidence. Reviewed the result as a contract synchronization; no application, schema, migration, dependency, or generated-file change was made. |
| 14 | Approve `IDEMPOTENCY-FENCING-A`: retain the lease `processingStartedAt`, lock and verify the claim inside the resource transaction, hold the lock through completion, fence stale old owners before mutation, and add PG-12. | Updated specification/API transaction ordering, unit/API evidence, PG-12, AC-65, and the Ticket-creation close gate so a stale old owner cannot mutate after reclaim. Reviewed the transaction ordering and preserved the no-FAILED-state decision without changing application or database source files. |
| 19 | Perform one final documentation-only Lab 2 contract sync to close the Category migration ambiguity and mark retry-backoff timing implementation-defined. | Updated the specification, API contract, and tests to require in-place Category migration preserving `id`, `name`, and `createdAt`, deterministic backfill actors, and `updatedAt = original createdAt`; kept the bounded randomized maximum-three-attempt Serializable policy while making exact backoff milliseconds implementation-defined. |
| 22 | Perform a focused documentation-only Lab 2 handout-authority correction pass for required test filenames, canonical AI-use naming, decimal Attachment size, and mandatory icon-only tooltips. | Updated the Lab 2 contracts and test plan for handout-required test ownership, the decimal Attachment limit and boundaries, and tooltip plus accessible-name evidence. This record preserves the prompt as historical activity while the current pass applies the superseding handout filename correction. |
| 23 (current) | Perform a final Lab 2 course-delivery consistency pass: rename the AI-use deliverable to `docs/lab-02/ai-use.md`, curate 6–10 selected prompts, and synchronize Issues #21, #24, and #25 to `tests.md`. | Used the handout and current `tests.md` as authority; renamed and curated this deliverable, then applied focused Issue edits for Ticket test ownership and the 12-hex Ticket Number suffix, the 5,000,000-byte Attachment limit and `attachments.api.test.ts`, and the current E2E file ownership. Verified no application, schema, migration, dependency, generated-file, or commit changes were made. |
| 24–26 (current) | Diagnose why merged PR #27 did not close Issue #17, then make merged-PR issue closure independent of the PR base branch. | Confirmed GitHub ignored `closes #17` because PR #27 targeted `lab2-staging`, and confirmed the existing workflow did not subscribe to merged PRs. Added a global merged-PR handler that applies the existing closing-keyword syntax with the scoped Actions token, then closed the already-merged Issue #17 as completed because its event cannot be replayed; no application, API, Prisma, or dependency changes were made. |
| 27 (current) | Re-audit all Lab 2 Issues against the latest contracts in `docs/lab-02/`. | Scrutinized live Issues #17–#26 against the current specification, API, UI, and test plan; updated #17, #18, #19, #22, #24, #25, and #26 for fixed handout filenames, decimal Attachment boundaries, mandatory icon-only tooltip evidence, and canonical `ai-use.md` naming. Confirmed #20, #21, and #23 required no changes and made no application-code changes. |
| 28 (current) | Implement Issue #18 Lab 2 data model, forward migration, seed, reference services, and guarded PostgreSQL evidence; set up a disposable Docker test database. | Added the Prisma schema, explicit in-place Category upgrade migration, PostgreSQL checks/indexes/enums/restrictive FKs, idempotent synthetic seed, minimal reference services/tests, guarded migration/Attachment/idempotency suites, and a disposable PostgreSQL Compose target. Prisma validation, generation, build, focused unit tests, migration/seed/preservation (3), Attachment (4), Idempotency (5), and the Lab 1 Category regression passed against the dedicated target; no commit was created. |
| 29 (current) | Scrutinize Issue #18 against `docs/lab-02`, then fix the blocking migration and test-database safety findings before shipping. | Reproduced silent over-length Category-name truncation, partial non-transactional migration effects, and a hostname-alias reset-guard bypass. Added atomic migration/preflight protection, conservative database-name guarding, complete Section 7 column/default/nullability/index evidence, Ticket constraint boundaries, and regression tests for preservation and rollback. The 22-test Issue close gate, Prisma validation, server build, and Lab 1 health/Category regressions passed against the guarded disposable target; no commit was created. |
| 30 (current) | Fix the remaining Issue #18 scrutinize findings before shipping. | Reproduced the destructive reset-helper connection mismatch and ambiguous test-marker acceptance, bound every mutating database-test helper to the validated test target, and expanded Attachment, idempotency, and synthetic Requester boundary evidence. The 22-test Issue close gate, three guard tests, server build, Prisma validation, and Lab 1 health/Category regressions passed against the guarded disposable target; no commit was created. |
| 31 (current) | Fix the remaining AC-64 constraint-evidence gap before shipping. | Expanded fresh-schema introspection to all six Lab 2 tables and all primary/unique constraints, and added PostgreSQL duplicate-boundary coverage for requester email, Category name, Related System name, Ticket public ID/number, and Attachment storage key. The guarded close gate passed 26/26, with server type checking, Prisma validation, and diff checks passing; no commit was created. |
| 32 (current) | Fix-then-ship the remaining Issue #18 scrutinize findings. | Updated the guarded PostgreSQL documentation and current schema notes, narrowed test-helper error handling, and made migration evidence assert exact foreign-key targets as well as restrictive actions. Reran the focused Lab 2 suites, Prisma validation, server build, Lab 1 health/Category regressions, and diff checks; no commit was created. |
| 33 (current) | Fix-then-ship the remaining Issue #18 findings, consulting the current Prisma documentation for the database URL configuration. | Closed the remaining test-workflow gaps by guarding both `DATABASE_URL` and Prisma CLI `DIRECT_URL`, correcting the documented disposable-database command order, and asserting the exact PostgreSQL SQLSTATE for each negative boundary. Updated the verified Lab 2 evidence statuses, reran the required PostgreSQL and Lab 1 regressions on disposable targets, and confirmed the server build; no commit was created. |
| 34 (current) | Fix-then-ship the remaining Issue #18 scrutinize findings. | Added database-level protection against hard-deleting bound Attachments, parameterized the disposable PostgreSQL password, reconciled the AI-use and test evidence counts, and reran the focused PostgreSQL, Lab 1 regression, Prisma validation, server build, and diff checks before shipping. |
| 35 (current) | Fix-then-ship the remaining Issue #18 scrutinize findings. | Added a forward migration and regression coverage that reject unbinding bound Attachments, represented the required summary/description trigram indexes in Prisma to prevent schema drift, recorded exact final verification evidence, and reran the focused PostgreSQL, Lab 1 regression, Prisma validation, and both application builds; no commit was created. |
| 36 (current) | Scrutinize the Issue #18 changes against the specification, then fix-then-ship the findings. | Removed the Attachment binding triggers through a forward `DROP TRIGGER`/`DROP FUNCTION` migration after probing showed they still allowed rebinding a bound Attachment to a different Ticket, resurrecting a Removed Attachment, and being bypassed by `TRUNCATE`; recorded in Specification Section 7.2.7 that the three CHECK constraints are the complete database-level contract and transitions are application-owned. Rewrote the affected regression to assert removal-metadata retention instead. Documented the `ticket_number` trigram expression index cast and the Lab 1 UTC timestamp interpretation. Corrected the verification evidence: the Lab 1 API tests resolve `DATABASE_URL`, not `TEST_DATABASE_URL`, and cannot share the disposable Lab 2 database because the guarded suite leaves fixture rows behind, so they now run against a second disposable database. Reran everything on throwaway PostgreSQL: empty drift, 0 triggers, focused gate 5 files/24 tests, full suite 8 files/30 tests, Lab 1 regression 2 files/2 tests, Prisma validation, and both builds. (Entry 37 later folded that `DROP TRIGGER` migration back into the single forward migration, which now never creates a trigger.) |
| 37 (current) | Scrutinize the Issue #18 changes against the specification, then fix-then-ship and combine the two migrations. | Reverted an uncommitted `describe.skip` fallback that let the three guarded PostgreSQL suites report green while skipping all 18 tests without `TEST_DATABASE_URL`, which contradicted `tests.md` Section 4.2.1 ("a missing/unsafe target fails the suite") and the Definition-of-Done rule that no required planned test is skipped. Folded the withdrawn `20260822110000_drop_attachment_binding_triggers` migration into `20260822000000_lab2_data_model` so the committed forward migration never creates the trigger that Specification Section 7.2.7 forbids, and recorded that rule in the migration header. Reran on throwaway PostgreSQL: 2 migrations, empty drift, 0 triggers, focused gate 5 files/24 tests, full suite 8 files/30 tests, Lab 1 regression, typecheck, and both builds. |
| 38 (current) | Apply the Karpathy guidelines to Issue #19, then plan and implement the Zen Green application shell and reusable UI foundation. | Confirmed the four open decisions before implementing: the Lab 1 Check System screen moves verbatim to `client/src/pages/SystemCheck.tsx` with only the import line of its Lab 1 test changed, `react-router-dom` and `@fontsource/inter` are added while Bootstrap remains the only UI library, the Section 31 component library is built now except the Attachment table/row state deferred to Issue #24, and unknown routes resolve to the `/error` 404 variant. Verified in `client/node_modules` that `var(--bs-primary)` has no consumers in the precompiled Bootstrap 5.3.8 stylesheet, so the token layer defines `--tt-*` tokens plus component-level `--bs-*` overrides and explicit rules for the hard-coded focus and checked literals. Shipped the shell, the sessionStorage requester context, the route guard, the standalone `/error` page, placeholder pages for Issues #20–#23, and 25 shell tests. Client suite 3 files/31 tests and the client build passed; the server was not touched and its suite was not run; no commit was created. |
| 39 (current) | Scrutinize the Issue #19 changes against `docs/lab-02`, then fix-then-ship the findings. | Reviewed the shell, token layer, and component library specification-first and reproduced five defects by tracing the compiled Bootstrap 5.3.8 stylesheet rather than assuming its behaviour: the fixed mobile drawer painted over the menu toggle that closes it, the always-mounted icon tooltip occupied layout permanently and rendered off-screen on the left-most control, `.modal-backdrop` at `z-index: 1050` never received the dismiss click under the full-viewport `.modal` at `1055`, `.modal-open` does not exist in Bootstrap 5 CSS so page scrolling was never locked, and `Uploading`/`Pending` shared one badge class that duplicated `.tt-badge--neutral` verbatim. Fixed each, dropped `aria-describedby` from `IconButton` because a display-toggled tooltip would leave it resolving to nothing in a browser while still passing in jsdom, removed the redundant `role="alert"` from `ValidationMessage`, and recorded the `<form noValidate>` requirement that the native `required` attribute creates for Issue #21. Left the unrouted Lab 1 Check System screen alone: no Lab 2 specification or handout defines that route. Client suite 4 files/57 tests and the client build passed; the server was not touched; no commit was created. |
| 40 (current) | Fix-then-ship the remaining Issue #19 review findings. | Added responsive 24/20/16px card spacing through the Zen Green token layer; moved IconButton tooltips into a viewport-positioned portal with edge clamping so Bootstrap modal overflow cannot clip them or create page-level scrolling; updated shell, modal, and filter-chip tests to exercise hover/focus visibility and body-level tooltip placement; reran the client suites/build and preserved the frontend-only scope. |
| 41 (current) | Fix the remaining Issue #19 review findings. | Cleared malformed stored requester context, restored the Lab 1 Check System compatibility route, changed Lab 1 tests to exercise the routed application, and added regression assertions before rerunning the frontend checks. |
| 42 (current) | Fix-then-ship the remaining Issue #19 review findings. | Restored focus to the mobile navigation toggle after drawer navigation, changed standalone error Back to the secondary treatment, and corrected this record's prompt/entry count. The focused shell suite (27 tests), full client suite (57 tests), client build, and diff check passed; no commit was created. |
| 43 (current) | Fix-then-ship the remaining Issue #19 form-style review findings. | Mapped Bootstrap invalid-control tokens to Zen Green's dark-red danger token, preserved the invalid border during focus, and added an editable-control hover state that excludes disabled, read-only, and invalid controls. Reran the focused shared-component/shell suites, full client suite, both application builds, and diff checks; no commit was created. |
| 44 (current) | Fix-then-ship the remaining Issue #19 scrutinize findings. | Made navigation active-state matching route-aware, including trailing-slash routes, and moved focus to the standalone requester page after Change Requester unmounts the shell. Reran the focused shell suite (28 tests), full client suite (58 tests), and both application builds. Did not silently resolve the documented `/system-check` compatibility-route versus mandatory `X-Requester-Id` contract conflict; no commit was created. |
| 45 (current) | Fix-then-ship the remaining Issue #19 route/API contract finding. | Removed the uncontracted `/system-check` route from the Lab 2 router and README, restored direct Lab 1 `SystemCheck` coverage, added a regression asserting `/system-check` resolves to the safe 404 route, and synchronized the Lab 2 evidence. Reran the focused Lab 1 and shell suites, full client suite (59 tests), both application builds, and diff checks; no commit was created. |
| 46 (current) | Fix-then-ship the remaining Issue #19 findings after the specification-first review. | Closed the mobile drawer on every in-shell router location change and restored focus to the toggle, added a reusable `Form` boundary that defaults to `noValidate`, and added regressions for both behaviors. Preserved the specs-first Lab 1 decision: `SystemCheck` remains directly tested, while `/system-check` is not added to the Lab 2 route contract. Verified the focused suites (55 tests), full client suite (61 tests), client/server builds, and diff hygiene; responsive/visual browser coverage remains assigned to Issue #25. |
| 47 (current) | Fix-then-ship the remaining Issue #19 scrutinize findings. | Changed requester-context validation to require positive safe integers, added an unsafe-ID route-guard regression, and corrected the stale Lab 2 evidence that incorrectly described Lab 1 coverage as routed. Reran the focused shell suite, full client suite, client/server builds, and diff checks; responsive/visual browser coverage remains assigned to Issue #25. |
| 48 (current) | Fix-then-ship the Issue #19 modal-focus and pagination scrutinize findings. | Closed the modal focus-trap escape on `Shift+Tab` from the dialog container, moved opening focus to the dialog itself so no dialog opens with the close control's tooltip showing, and clamped `Pagination` to a valid page so a narrowed result set cannot render a range past its end. Added three regressions and verified each fails against the pre-fix component. Reran the shared-component suite (32 tests), full client suite (72 tests), client typecheck/build, and diff checks; responsive/visual browser coverage remains assigned to Issue #25. |
| 49 (current) | Fix-then-ship the Issue #19 persistent-sidebar and drawer-containment scrutinize findings. | Made the desktop sidebar `sticky` so Change Requester stops sinking to the document bottom on a long ticket list, marked the main region `inert` while the mobile drawer is open so Tab stops walking behind the backdrop, and gave `Invalid` its own dashed badge so no two Attachment states differ only by their label. Added three regressions and verified each fails against the pre-fix component. Reverted an attempted focus gate on Requester Selection: react-router v7 wraps `navigate` in a transition, so `RequesterGuard`'s synchronous redirect always wins and the flag never arrives. Corrected the stale test counts and the falsely-passing `git diff --check` row in the prior entry. Reran the full client suite (74 tests), typecheck, build, and diff checks; responsive/visual browser coverage remains assigned to Issue #25, which must now also verify the sticky sidebar in a real browser. |
| 50 (current) | Fix-then-ship the Issue #19 scrutinize findings without implementing later-lab screens. | Kept the spec-defined route set, documented the Lab 1 direct-component compatibility boundary and Issue #22 responsive-table deferral, and included the pending disabled bare-icon CSS correction. The focused and full client checks plus both application builds were rerun before committing; browser responsive/visual coverage remains assigned to Issue #25. |
| 51 (current) | Fix-then-ship the two actionable Issue #19 scrutinize findings while deferring responsive/visual browser verification as requested. | Replaced per-page POP focus logic with a persistent route-focus boundary that handles history returns without overriding shell drawer focus; merged caller `aria-describedby` values while keeping generated error/counter/invalid wiring authoritative across `TextInput`, `Select`, and `Textarea`. Added regressions and verified the focused/full client suites plus client/server builds; responsive/visual browser coverage remains assigned to Issue #25, and no commit was created. |
| 52 (current) | Fix-then-ship the remaining Issue #19 error-route focus and icon-tooltip evidence findings. | Added a focus target to the standalone `/error` main, covered live error-route focus restoration, and covered tooltip visibility on keyboard focus/blur for the mobile menu toggle. Focused/full client tests and both application builds were rerun; manual/responsive browser checks remain intentionally deferred, and no commit was created. |
| 53 (current) | Requested a step-by-step implementation plan for resolving the PR #32 review findings. | Used the plan to synchronize the Issue #19 ownership wording (shell/responsive foundation under #19, API-backed My Tickets under #22, final Playwright responsive/visual verification under #25), the current UI-05/UI-31/UI-35 test traceability, and this record's prompt/entry counts, without adding Issue #22 or Issue #25 functionality. |
| 54 (current) | Implement Issue #20: the Development Requester context middleware, the `GET /api/requesters` bootstrap endpoint, the Requester Selection screen and its API funnel, then run every gate and record the evidence. | Built the server middleware stack (`requestLog`, `cors`, `transport`, `requesterContext`) and the shared error contract, added `developmentRequesterService` and the `referenceData` routes so `GET /api/requesters` answers without a requester header while every other `/api` route except `GET /api/health` is guarded, and added the client `apiFetch`/`useRequesterApi` funnel and the real Requester Selection screen. Verified on 2026-08-24 against two disposable PostgreSQL 16 databases, never the development database: server focused gate 6 files/44 tests, Lab 1 regression 2 files/2 tests, full server suite 14 files/74 tests, server build, client focused gate 2 files/65 tests, full client suite 5 files/106 tests, client build, and a live `curl` smoke check showing `GET /api/requesters` 200 with `Cache-Control: no-store` and only the four active synthetic Requesters, `GET /api/categories` 400 with the `X-Requester-Id` details marker, and `GET /api/health` still exempt. Deliberately deferred: the `CategoryDTO` conversion to Issue #21, `GET /api/tickets` and its `X-Pagination` value to Issue #22, and responsive/visual browser evidence to Issue #25; the browser half of the Step 3 smoke check was driven manually by the developer rather than by automation, because this environment has no browser driver and Playwright is out of scope; all five behaviors passed. This entry also closes the compatibility conflict recorded in entry 44: the `/system-check` versus mandatory `X-Requester-Id` conflict no longer exists, because `/api/categories` is now behind the requester guard and the Lab 1 `SystemCheck` page is unrouted and therefore unreachable from the Lab 2 application. No commit was created.
| 55 (current) | Close the remaining Issue #20 contract gaps before starting Issue #21: give invalid Requester context a machine-readable error code, add `reference-data.api.test.ts` to the Issue #20 gate, correct the requester-switching ownership boundary, make shared transport ownership explicit, and split bootstrap from requester-scoped `Vary`. | Added the protocol-specific code `REQUESTER_CONTEXT_INVALID` (`400`) to the centralized error catalogue and switched the requester guard onto it, so all six failure conditions return one safe indistinguishable envelope and the client keys its clear-and-redirect rule on the `code` instead of a `details` field marker; an ordinary application `400` no longer clears the Requester. Synchronized `specification.md` (BR-15/BR-15a, BR-17/BR-17a, AC-41, AC-46, AC-60, Section 8.1, the error-code list), `api-spec.md` (Sections 3.1, 3.6, 4.2, 4.3, 6.1, 15), `ui-spec.md` (Sections 5.3, 5.4, summary), `tests.md` (API-01, API-72, API-74, UI-04, the Issue #20 close-gate row, new Section 4.5.19), `README.md`, and GitHub Issue #20. Issue #20 now owns the shared invalidation mechanism and its registration seam, while Issues #21-#24 keep their own feature-specific state clearing. Verified on 2026-08-25: server focused gate 6 files/46 tests, client focused gate 2 files/66 tests, full Lab 2 server suite 12 files/74 tests, full Lab 2 client suite 3 files/101 tests, both builds. Deliberately not done: no new authentication, framework, validation library, or downstream Issue #21-#24 product behavior; the guarded PostgreSQL suites, manual browser smoke, and `curl` smoke were not re-run because no schema, seed, or UI flow changed. |
| 56 (current) | Commit the Issue #20 contract cleanup and start implementing Issue #21. | Built the Issue #21 backend and Create Ticket screen. Server: moved `GET /api/categories` onto the shared reference-data router and widened it to the full `CategoryDTO`, added `GET /api/related-systems`, and added `POST /api/tickets` following the api-spec Section 8.2.1 order — parse, canonicalize, hash, resolve the claim, then mutate — with `IDEMPOTENCY-FENCING-A` locking the claim row and verifying status, hash, and the exact retained `processingStartedAt` before any Ticket or Attachment mutation. The canonical hash sorts lowercased `attachmentIds` so `[A,B]` equals `[B,A]`; `PROCESSING_LEASE_SECONDS = 300` with reclaim at `now >= processingStartedAt + 300s` as one atomic conditional update; a controlled failure removes the owned claim rather than storing FAILED. Extracted `runCreateTicket` from the route so the PostgreSQL concurrency suites exercise the same orchestration a real request does over separate connections. Client: the Create Ticket form with API-loaded Category/Related System, read-only Ticket Number/Ticket Date/Requester, field validation with first-invalid focus, 4xx retention with backend field mapping, ambiguous-5xx recovery persisted to `sessionStorage` and resumed only by explicit user action, and the payload-bound Idempotency Key lifecycle. Verified on 2026-08-25: server focused gate 8 files/166 tests, client focused gate 1 file/30 tests, full server suite 20 files/239 tests, full client suite 6 files/137 tests, Lab 1 regression server 2/2 and client 2/6, both builds. Deliberately deferred: the Attachment upload lifecycle and collection delete to Issue #24, `GET /api/tickets` to Issue #22, Ticket Detail to Issue #23, and responsive/visual/Playwright evidence to Issue #25. This entry also records the one Issue #20 code gap the contract cleanup exposed and closed: `transport` was adding `X-Requester-Id` to `Vary` on every response including the bootstrap, which the requester guard now sets instead. |
| 57 (current) | Perform the PR #34 non-blocking cleanup: remove accidental `.gitignore` noise, make verification wording historically precise, synchronize approved counts, and leave Issue #21 behavior plus Issues #22–#24 scope unchanged. | Removed the duplicate generated-directory rule and stray `ignore` rule, updated Section 4.5.22 and PR #34 to identify `8c0b772bf39e97848b6f26a78c827df351bce372` as the last executable-code verification commit, and synchronized the current server 254 / PostgreSQL 36 / client 150 counts. No application or test source changed; no application suite was rerun because cleanup stayed documentation/repository-only. |
| 58 (current) | Implement Issue #22 requester-owned My Tickets: `GET /api/tickets` with ownership/deleted predicates before search, filters, sorting, counting, and pagination; a bounded validated query contract feeding a reusable generic QueryBuilder; `TicketListItemDTO[]` plus `X-Pagination`; and the API-backed My Tickets screen with its distinct states. | Confirmed three open decisions before implementing: the filter modal uses native `<select multiple>` per the ui-spec Section 14.2 mockup, the committed query lives in the URL through `useSearchParams`, and verification runs the complete suites against a live PostgreSQL target. Added `services/queryBuilder.ts` (twelve generic conditions, one OR search group, generic ordering, unit-tested against a fictional resource so no Ticket rule can hide in it), `services/ticketQueryValidator.ts` (field whitelists, the exact condition matrix including `ISNULL`/`ISNOTNULL` rejection, typed conversion, `IN` 1–100 uniqueness, the 200-character and 20-expression bounds, the internal `id DESC` tiebreaker, and pagination bounds), `services/ticketListService.ts` (Requester ownership, `deleted = false`, the bounded `select`, historical Category/System names), and `http/pagination.ts`. Established that semantic Priority ordering needs no CASE expression because the migration declares the enum as `('LOW','MEDIUM','HIGH')` and PostgreSQL orders enum columns by declaration order, and pinned that with a named test. Added `onResponse` to `apiFetch` so one caller can read `X-Pagination` without a second fetch wrapper, and rewrote `MyTickets.tsx` with a 400 ms debounce, draft/apply/cancel/reset filters, removable chips, sort-preserving Clear Filters, and distinct loading, true-empty, no-results, invalid-query, and failure states. Updated the existing client fetch doubles, which had no `headers`, and corrected an ambiguous `getByRole("navigation")` query now that the list renders its own pagination landmark. Recorded that `userEvent` deadlocks under Vitest fake timers, so the debounce test drives the change event directly. Server 370 tests across 19 non-PostgreSQL files, client 183 tests across 7 files, and both builds passed; the guarded PostgreSQL suites were not run in that pass. No commit was created. |
| 59 (current) | Scrutinize the Issue #22 change end-to-end and apply the fixes it surfaced. | Traced the route through validator, QueryBuilder, service, and Prisma arguments, and the client from URL query through `buildTicketListSearch` and `onResponse` to each render state. Confirmed clean: `GET /tickets` is not exempt from `requireRequesterContext`, the ownership and `deleted = false` predicates are fixed `AND` members shared by `findMany` and `count`, the bounded `select` makes the projection structural, `Object.hasOwn` keeps a prototype key out of the condition matrix, `mode: "insensitive"` is correctly withheld from `IN`, and `setSearchParams` is stable per URL so the debounce cannot loop. Fixed two findings. `MyTickets.tsx` hid the pagination controls on a row count of zero, which stranded a page past the last one -- a `200` with an empty array under BR-38 -- on the no-results state with no control left to correct it; the condition now reads the total, so `Pagination` stays mounted and reports its clamp back. The "sorts Priority semantically" test claimed to catch a reordered enum but asserted only the identity passthrough, which holds whatever the migration declares; the premise is now read out of the `CREATE TYPE "RequestedPriority"` DDL, the only authority on the order PostgreSQL sorts by. Both new cases were confirmed to fail against the pre-fix code and the restored migration leaves `git diff prisma/` empty. Server focused gate 4 files/170 tests, client focused gate 1 file/36 tests, server non-PostgreSQL 19 files/379 tests, client 7 files/186 tests, both typechecks. Recorded in `tests.md` Section 4.5.24, which supersedes the stale counts in Section 4.5.23. Deliberately not fixed and recorded instead: the absent PostgreSQL coverage of the list path, unescaped LIKE wildcards in search terms, and the roleless focusable `<tr>`. The guarded PostgreSQL suites were not re-run because no target was available to this pass and neither fix touches schema, migration, seed, or query construction. No commit was created. |
| 60 (current) | Scrutinize the Issue #22 change end-to-end, then apply the fixes that review produced. | Traced the route through validator, QueryBuilder, service, and Prisma arguments, and the client from URL query through `buildTicketListSearch` and `onResponse` to each render state. Confirmed clean and unchanged: `GET /tickets` is not exempt from `requireRequesterContext`, ownership and `deleted = false` are fixed `AND` members shared by `findMany` and `count`, the bounded `select` makes the projection structural, `Object.hasOwn` keeps a prototype key out of the condition matrix, `mode: "insensitive"` is withheld from `IN`, `table-layout: fixed` plus `overflow-wrap: anywhere` is what actually prevents page-level horizontal scroll, and the mobile column set matches ui-spec Section 30.3 exactly. Fixed five findings. The pagination clamp shared the user's page-change callback and therefore pushed, leaving the out-of-range address in history so Back landed on it, clamped, and pushed again -- a page the user could never navigate back past; a correction now replaces. The same passive-effect timing let both empty states paint for a page about to be corrected, so "No tickets found" appeared with a Clear Filters button on an address with no search or filter; one predicate now guards both. `MAX_PAGE_NUMBER = 1_000_000` contradicted api-spec Section 9.12, which states `pageNumber >= 1` and makes an out-of-range page a `200` with an empty array, and its overflow justification did not hold at that value; the constant is removed and `ticketListService.ts` now skips only the row read -- keeping the count, so `X-Pagination` stays complete -- when `skip` would leave the safe-integer range, so code moved to the specification rather than the specification to the code. Datetime filter values are now checked for ISO-8601 shape before `new Date`, which alone read `"5"` as 2001-05-01. A repeated `searchFields` reports one detail instead of two. Every new case was confirmed to fail against restored pre-fix source (8 server, 1 client) before the fixed files were put back. Server focused gate 4 files/181 tests, server non-PostgreSQL 19 files/390 tests, client 7 files/187 tests, both typechecks, both builds. Recorded in `tests.md` Section 4.5.25, which supersedes the counts in Section 4.5.24. Deliberately not fixed and recorded instead: the absent PostgreSQL execution coverage of the list read path, and the unescaped LIKE wildcards in search terms, whose ceiling and upgrade path are now marked at `queryBuilder.ts` rather than living only in this log. Then established that the `28P01` failure recorded in the two previous passes was never an unreachable target: the `toktickit-lab2-test-postgres` container was listening on `localhost:55432` the whole time and only the `TEST_DATABASE_URL` password in the untracked `.env.local` had drifted from the container's `POSTGRES_PASSWORD`. Supplying the container's own credential ran all four guarded suites green -- 4 files, 36 tests -- so the earlier "not run" rows recorded a stale local credential rather than a missing environment. That does not close the outstanding finding: the harness works and simply has no case touching `GET /api/tickets`, which turns the largest remaining unknown from an unavailable target into unwritten coverage. No schema, migration, seed, or query construction changed. No commit was created. |
| 61 (current) | Scrutinize the Issue #22 change end-to-end again, then apply the fixes that review produced. | Re-traced the route through validator, QueryBuilder, service, and Prisma arguments, and the client from URL query through `buildTicketListSearch` and `onResponse` to each render state, then re-checked the whitelists and the condition matrix line by line against api-spec Sections 9.5 and 9.7 and the mobile column set against ui-spec Section 16.3; all matched. Fixed five findings. `routes/tickets.ts` passes `req.requesterId` through an `as number` cast, and Prisma reads `undefined` in a `where` as "predicate not supplied", so an unresolved Requester would have collapsed the ownership predicate to `{}` and answered `200` with every Requester's rows and counts; `listTicketsForRequester` now rejects a requesterId that is not a positive safe integer, closing the class at the one place all callers route through rather than at the route. The pagination footer derived its range from a total the caller clears before each request, so it painted "Showing 0-0 of 0" underneath the skeleton rows, and because that line was also an `aria-live` region it announced the loading state as an empty result on every search pause, sort change, and page click; the range now yields to a height-preserving placeholder while a fetch is pending and `Pagination` is no longer a live region at all. The loading announcement was mounted only while loading, which is the shape `RequesterSelection.tsx` already documents as unreliable -- a `role="status"` node inserted with its text present is announced inconsistently -- and `MyTickets.test.tsx` asserted its absence after load, locking that in; the region is now always mounted with only its text changing, reusing the existing pattern instead of re-deriving one, and it carries the result count the removed live region used to announce. String filter values had no length bound while `search` was capped at 200, so only Node's request-line limit stood between a hand-built URL and a multi-kilobyte `contains` term long enough to defeat the trigram indexes; they are now capped at `MAX_FILTER_VALUE_LENGTH = 2000`, the longest filterable column, counted in code points and applied to every `IN` element. Every new case was confirmed to fail against restored pre-fix source (3 server, 2 client) before the fixed files were put back. Server focused gate 4 files/184 tests, server non-PostgreSQL 19 files/393 tests, client 7 files/188 tests, both typechecks, both builds. Recorded in `tests.md` Section 4.5.26, which supersedes the counts in Section 4.5.25. Deliberately not fixed and recorded instead: the absent PostgreSQL execution coverage of the list read path -- narrowed but not answered by the requesterId guard, which proves the predicate cannot be dropped, not that PostgreSQL applies it as intended -- and the unescaped LIKE wildcards. One finding was raised and left open as unverified: `<fieldset disabled={loading}>` drops keyboard focus to `<body>` on every fetch, which follows from the HTML disabled-fieldset rule but was not observed, because jsdom does not model it and both available fixes trade the specification-mandated `disabled` for a hand-rolled substitute. The guarded PostgreSQL suites were not run -- no target is reachable from this session, and the three files fail at connection with `28P01` -- and no schema, migration, seed, or query construction changed. No commit was created. |
| 62 (current) | Close the PostgreSQL coverage gap the three previous Issue #22 review passes recorded but left open, on the feature branch, without merging. | Added `tests/lab-02/postgres/my-tickets.postgres.test.ts` (PG-15), which drives `listTicketsForRequester` through `parseTicketListQuery` so each case exercises the real validator to QueryBuilder to Prisma path against a live engine. The fixture is built to be returned wrongly: two Requesters whose Tickets match each other's search terms and filters, a logically deleted row that would match an active search, a Ticket on reference rows that later go inactive and deleted, and forty rows sharing one creation instant. Ownership is now an outcome rather than the presence of a `{ requesterId }` object; `mode: "insensitive"` is shown to reach the comparison for `contains` and for `not` and against the `CHAR(25)` `ticket_number`; Priority is shown to sort by enum declaration order rather than alphabetically, where previously it was pinned only by reading the DDL; Description-only matching, historical Category and Related System names, the search-OR-group-AND-filters composition, and paging past a wide tie and past the final page are all executed. Non-vacuity was established per fault rather than in bulk, because removing the scope predicate fails all eleven cases at once and isolates nothing: clearing `caseInsensitive` alone fails exactly the five case-sensitivity-dependent cases and leaves the other six passing. One claim resisted falsification and is recorded as such rather than implied: deleting the `id DESC` tiebreaker leaves every case passing, first at two tied rows and then at forty paged in sevens written specifically to break it, because PostgreSQL's sort is deterministic for a given plan and no fixture can make it rearrange tied rows on demand -- the tiebreaker guards behavior PostgreSQL leaves unspecified across plan changes, which is the shape of claim a fixture cannot refute. An earlier draft of that case asserted an exact row order and claimed to prove the tiebreaker; it proved nothing, and was rewritten to assert only the complete duplicate-free walk it does own, with the measurement recorded in the test. PG-15 added to the traceability matrix. New suite 11 tests, all guarded PostgreSQL suites 5 files/47 tests, server non-PostgreSQL 19 files/393 tests, client 7 files/188 tests, both typechecks. Recorded in `tests.md` Section 4.5.27, which supersedes the counts in Section 4.5.26. Still open: unescaped LIKE wildcards, now unblocked by this coverage, and the `<fieldset disabled>` keyboard-focus finding, which needs the real browser Issue #25 owns. Committed and pushed to `feature/22-my-tickets`; not merged. |
| 63 (current) | Scrutinize the Issue #22 change end-to-end, apply the fixes it produced, then close every item the four previous passes had recorded as deliberately open. | Traced the route through validator, QueryBuilder, service, and Prisma arguments, and the client from URL query through `buildTicketListSearch` and `onResponse` to each render state, and executed rather than read: 22 hand-built query strings through `parseTicketListQuery`, and all five guarded PostgreSQL suites against a live `postgres:16-alpine`. Confirmed clean: ownership and `deleted = false` as fixed `AND` members shared by both reads, the bounded `select`, `Object.hasOwn` blocking a prototype key, `mode: "insensitive"` withheld from `IN`, the LIKE-wildcard escape, the `id DESC` tiebreaker, the skip-overflow guard, `table-layout: fixed` preventing page-level horizontal scroll, and `IconButton` carrying both a programmatic name and a visible tooltip. Four review findings fixed. A datetime filter value was shape-checked and then trusted to `new Date`, which rejects an out-of-range month but rolls an out-of-range *day* forward, so `createdAt GREATER "2026-02-30"` answered `200` with rows from `2026-03-02`; the pattern now captures the date parts and a leap-year-aware arithmetic check reads them, deliberately not a second `Date` round trip because `Date.UTC(26, 0, 1)` is 1926. Created At rendered the UTC date while the Ticket Number beside it carries the `Asia/Bangkok` business date, so one row disagreed with itself for seven hours a day; it now formats through a pinned `en-CA`/`Asia/Bangkok` formatter. A dropped `X-Pagination` header left a derived zero standing in for the total, which showed "No tickets found" to a Requester who had none and stripped every pagination control from a full page of rows; both sites now fall back to the row count. `query` was an unused dependency of the list effect. Then closed the four standing deferrals. The `CHAR(25)` to `VARCHAR(25)` change had been made by editing `20260822000000_lab2_data_model` after it was applied, which changes its checksum and fails `prisma migrate deploy` against any database that ran it -- the practice AGENTS.md Section 9 forbids; that migration is restored byte-for-byte and the change moved to the forward migration `20260826000000_ticket_number_varchar`, which drops the index *before* the type change so `ALTER COLUMN ... TYPE` cannot rebuild it at its own `(ticket_number::text)` expression and leave it as unreachable as before. The roleless focusable `<tr>` became a real `<Link>` in the Ticket Number cell, which ui-spec Section 16.2 permits explicitly and which a `role` on a `<tr>` could not deliver without breaking table row semantics. The `<fieldset disabled>` keyboard-focus finding was closed by deleting the disabling rather than substituting for it: it bought nothing the list effect's `ignore` flag and `Pagination`'s `pending` did not already provide, and it threw a keyboard user out of the toolbar every 400 ms while typing. The page read and the count now share one `REPEATABLE READ` transaction, with the isolation level asserted by name because the default `READ COMMITTED` snapshots per statement and would have satisfied a weaker assertion while changing nothing. Every new case was confirmed to fail against restored pre-fix source: 5 server and 3 client for the review findings, 1 server and 4 client for the deferrals. Server non-PostgreSQL 19 files/415 tests, client 7 files/195 tests, all guarded PostgreSQL suites 5 files/49 tests, both typechecks, and the two-migration chain applied from empty and read back. Recorded in `tests.md` Section 4.5.29, which supersedes the counts in Section 4.5.28. Still open and recorded rather than fixed: text-field `IN` is up to 100 `ILIKE` comparisons against no usable index, bounded by the cardinality cap and the scope predicate and not emitted by the Requester UI. **Process failure recorded in full:** `npx prisma migrate deploy` was run with `DATABASE_URL` exported to the disposable local container, on the assumption that this retargeted it. It did not. `prisma.config.ts` resolves the migration connection as `process.env.DIRECT_URL || env("DATABASE_URL")`, and `.env.local` sets `DIRECT_URL` to the shared Supabase direct connection, so the export was silently ignored and the new migration was applied to Supabase without authorization. The table was empty and the Lab 2 migration had already been restored to its applied bytes, so no checksum broke and no data was at risk -- ordering and luck, not care. On instruction the change was reverted inside one transaction (index dropped, column returned to `CHAR(25)`, expression index recreated, `_prisma_migrations` row deleted) and the restored state verified read-only: `character(25)`, `gin (((ticket_number)::text) gin_trgm_ops)`, and `migrate status` reporting the forward migration as not yet applied. AGENTS.md Section 9 now records the `DIRECT_URL` precedence, that `TEST_DATABASE_URL` does not affect the Prisma CLI, and that `prisma migrate status` names its target read-only and is the way to check before running anything that writes. |

## Reflection
The full Lab 2 history is retained in the conversation/repository context, but
the handout-facing deliverable intentionally selects forty representative
prompts in thirty-eight table entries rather than reproducing every wording
correction. The selection
covers specification and API contracts, UI behavior, schema and migration
decisions, Attachment lifecycle, idempotency/concurrency, test traceability,
and course-delivery compliance. Every retained item corresponds to an actual
AI-assisted activity available in the repository or current session; no prior
prompt history outside that context was reconstructed.

The Lab 2 filename follows the handout's explicit `ai-use.md` requirement,
while Lab 1's historical `ai_use.md` remains unchanged. The selected records
also show the review boundary used throughout Lab 2: documentation and Issue
contracts were synchronized against the authoritative specification and
`tests.md`, while application implementation decisions were not silently
changed during these consistency passes.
